/**
 * Shared Types
 * 
 * Re-exporting all types from core and adding shared types needed by both
 * extension and webapp.
 */

// Re-export all core types
export * from '../../core/types';

// Export bridge types
export * from './bridge';

// Export recording types
export * from './recording';
