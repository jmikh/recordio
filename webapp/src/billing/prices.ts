/**
 * Single per-seat plan prices in $/seat/month (billing revamp Step 1).
 * PRICE_YEARLY is the effective monthly rate when billed annually.
 * Display-only — the server/Stripe owns the real amounts.
 */
export const PRICE_MONTHLY = 15;
export const PRICE_YEARLY = 12;
