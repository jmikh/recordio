/**
 * Minimum window duration in milliseconds.
 * Used for both:
 * 1. Drag constraints - prevents resizing windows below this duration
 * 2. Split visibility - scissors button only shows when playhead is at least this far from edges
 */
export const MIN_WINDOW_DURATION_MS = 500;
