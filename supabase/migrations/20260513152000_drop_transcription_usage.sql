-- Transcription usage/quota tracking removed. Cloud transcription is available
-- to any workspace with an active subscription, with no per-user minute limits.
DROP TABLE IF EXISTS public.transcription_usage;
