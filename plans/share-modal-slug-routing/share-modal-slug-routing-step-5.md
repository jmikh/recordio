# Step 5 — Share modal UI + header swap + dashboard tags

- **New** `webapp/src/editor/components/header/ShareModal.tsx`: reads `useProjectMetaStore`; owner gets — Invite people (typeahead over workspace-get members, excluding owner + already-granted; picks call `project-editor-set` with default Can view), visibility Dropdown (Private / Everyone in workspace / Anyone with the link), an Everyone-in-workspace row with Can view|Can edit when policy ≠ private, per-editor role Dropdown + XButton, Copy link. Policy/access changes that would erase grants show an inline confirm first (server enforces the rule regardless). Optimistic updates with revert-on-error. Mux publish: fire-and-forget `mux-video-create` on private→shareable and on Copy link (skipped while media is syncing; caller must have edit access). Non-owner: static labels, no controls, note "Only the owner can change share settings", Copy link enabled.
- `Header.tsx`: Publish split-button (raw `<button>`s) → one `Button` "Share" opening the modal (free tier keeps the hard gate → ProUpgradeModal); the redundant project-get effect, `shareSlug` state, and `handleShare` are deleted (`trackPublishClicked/Failed` move into the modal).
- `ProjectCard.tsx`: Copy-link only when `sharePolicy` is workspace/public, else "Private" label (replaces "Draft" — every project has a slug now); "Shared with you" badge when `sharedWithMe`.
- `DashboardPage.tsx`: passes `sharePolicy` + `sharedWithMe` (`editorRole && ownerId !== userId`) into the grid cards.
